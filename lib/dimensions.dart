import 'package:flutter/material.dart';
import 'package:sizer/sizer.dart';

class CrunchDimensions {
  static final roundBorderRadius = BorderRadius.circular(4.w);
  static final circleBorderRadius = BorderRadius.circular(100);
  static final verticalPadding = EdgeInsets.symmetric(vertical: 8.h);
  static final horizontalPadding = EdgeInsets.symmetric(horizontal: 8.w);
}
